name: simple workflow
on: workflow_dispatch
jobs:
    first:
        runs-on: ubuntu-latest
        steps:
            - name: echo1
              run: echo Hello
            - name: echo2
              run: echo World